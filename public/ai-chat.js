(()=>{
const chat=document.getElementById('chat');if(!chat)return;const body=chat.querySelector('.chatbody'),input=chat.querySelector('.chatinput'),headSmall=chat.querySelector('.chathead small');if(headSmall)headSmall.textContent='AI ordering & reservations';body.innerHTML='';input.innerHTML='<input id="chatText" placeholder="Order food or reserve a table naturally"><button id="chatSend">Send</button>';
const agentState={stage:'idle',name:'',phone:'',email:'',notes:''};let reservationState={},reservationMode=false,busy=false;
function esc2(s){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}function say2(t,c='bot'){body.insertAdjacentHTML('beforeend',`<div class="${c}">${esc2(t).replace(/\n/g,'<br>')}</div>`);body.scrollTop=body.scrollHeight}function catalogProduct(name){return menu.find(x=>x.name===name)||deals.find(x=>x.name===name)}function currentCart(){return [...cart.values()].map(({name,price,qty})=>({name,price,qty}))}function applyCapture(c){if(!c)return;for(const k of ['name','phone','email','notes'])if(String(c[k]||'').trim())agentState[k]=String(c[k]).trim()}function applyActions(actions){for(const a of actions||[]){if(a.type==='clear'){cart.clear();continue}const p=catalogProduct(a.item_name);if(!p)continue;if(a.type==='add')add(p,a.quantity||1);else if(a.type==='remove'){const x=cart.get(p.name);if(!x)continue;x.qty-=Math.max(1,Number(a.quantity)||1);if(x.qty<=0)cart.delete(p.name)}}renderCart()}function resetAgent(){Object.assign(agentState,{stage:'idle',name:'',phone:'',email:'',notes:''})}function soundsReservation(s){return /reserv|table|booking|book a|baith|beth|seat|dinner.*people|lunch.*people|logon?\s*(ke|k liye)|people\s*(for|at)|guests?/i.test(s)}
async function reservationTurn(message){const r=await fetch('/api/reservation-agent',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message,state:reservationState})}),j=await r.json();if(!r.ok)throw new Error(j.error||'Reservation assistant unavailable');reservationState=j.state||reservationState;if(j.reply)say2(j.reply);if(j.availability&&!j.created)say2(j.availability.available?`Tables are available. This party needs ${j.availability.tables_needed} table${j.availability.tables_needed===1?'':'s'}.`:`That slot is full for this party size. Please choose another time.`);if(j.created||j.done){reservationMode=false;reservationState={}}}
async function orderTurn(message){const r=await fetch('/api/agent',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message,cart:currentCart(),state:agentState})}),j=await r.json();if(!r.ok)throw new Error(j.error||'AI assistant unavailable');applyActions(j.actions);applyCapture(j.capture);agentState.stage=j.next_stage||agentState.stage;if(j.reply)say2(j.reply);if(j.submit_order){if(!cart.size||!agentState.name||!agentState.phone||!agentState.email){say2('I still need the missing order details before I can place this order.');return}const out=await submitOrder({customer_name:agentState.name,phone:agentState.phone,email:agentState.email,notes:agentState.notes||''});if(out.ok){say2(`Order #${out.orderId} has been placed successfully and sent to the kitchen queue. A confirmation email will be sent to ${agentState.email}.`);cart.clear();renderCart();resetAgent()}else say2(`I could not place the order: ${out.error}`)}}
async function handle(message){if(busy)return;busy=true;const btn=document.getElementById('chatSend');if(btn)btn.disabled=true;try{if(reservationMode||soundsReservation(message)){reservationMode=true;await reservationTurn(message)}else await orderTurn(message)}catch(e){say2('The AI assistant is temporarily unavailable. Please try again shortly.')}finally{busy=false;if(btn)btn.disabled=false}}function send2(){const el=document.getElementById('chatText'),q=el.value.trim();if(!q||busy)return;say2(q,'user');el.value='';handle(q)}document.getElementById('chatSend').onclick=send2;document.getElementById('chatText').onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();send2()}};say2('Hi! You can order food or reserve a table in your own words. For reservations, Mr. Feast has 5 joinable tables with 4 chairs each.');
})();
(()=>{const s=document.createElement('script');s.src='/reservation.js';s.defer=true;document.body.appendChild(s)})();
